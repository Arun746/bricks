name: directories
description: Initialize project with core folder structure, assets, and latest dependencies
version: 0.2.0

environment:
  mason: ">=0.1.0"

vars:
  project_name:
    type: string
    description: Project name (package name)
    default: my_project
    prompt: What is your project name?